name: flutter_lru_cache
description: flutter/dart lru cache.
version: 1.0.2
repository: https://github.com/hucong730/lru_cache

environment:
  sdk: ^3.5.4

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
